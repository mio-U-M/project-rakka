import { TimelineMax } from 'gsap';
import { slideController } from '@/lib/slideController';

export default class carouselController {
    constructor(carouselDom) {
        this.carousel = carouselDom;
    }
}